{
  "name": "e2e",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "E2E tests for @ocavue/utils",
  "scripts": {
    "pretest": "pnpm -w build",
    "test": "vitest",
    "build": "node build.js && prettier --write dist"
  },
  "devDependencies": {
    "@ocavue/utils": "workspace:*",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rslib/core": "^0.22.0",
    "esbuild": "^0.28.2",
    "prettier": "^3.9.6",
    "rolldown": "^1.2.4",
    "rollup": "^4.62.4"
  }
}
